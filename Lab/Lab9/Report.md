![alt text](screenshots/01-tree.png)

![alt text](screenshots/02-get.png)
```
SELECT 
    c.id, 
    c.body, 
    c.created_at, 
    u.name AS author_name
FROM children c
JOIN users u ON c.author_id = u.id
WHERE c.parent_id = %s
ORDER BY c.created_at
```
JOIN нужен для того чтобы объединить данные из двух таблиц.

![alt text](screenshots/03-create.png)
201 Created - код (HTTP-статус) используемый для создания ресурса.
Content-Type: application/json - HTTP-заголовок, оповещающий, что данные отправляются в json формате

![alt text](screenshots/04-update.png)
POST - используется на создание ресурса, а PUT - на обновление или замену существующего по его идентификатору.
При создании комментарий добавляется в коллекцию поста, поэтому нужен post_id, а при обновлении достаточно уникального comment_id, так как он уже существует в базе.


![alt text](screenshots/05-delete.png)
POST: 201 Created, потому что запрос успешно создал новый ресурс на сервере.
PUT: 200 OK, так как сервер корректно обновил существующую запись по её идентификатору.
DELETE: 204 No Content, поскольку ресурс удалён и тело ответа намеренно оставлено пустым.
GET: 200 OK, потому что сервер успешно извлёк и передал клиенту запрашиваемые данные.

![alt text](screenshots/06-errors.png)

![alt text](screenshots/06-errors-1.png)

![alt text](screenshots/06-errors-2.png)

![alt text](screenshots/06-errors-3.png)
404 Not Found означает, что запрашиваемый ресурс (например, комментарий с указанным ID) отсутствует в базе данных.
422 Unprocessable Entity означает, что сервер понял запрос, но данные не прошли проверку.

![alt text](screenshots/07-swagger.png)

![alt text](screenshots/08-vanilla.png)

Функция esc() экранирует пользовательский ввод, превращая опасные символы вроде < и > в безопасные HTML-сущности, чтобы предотвратить выполнение вредоносного кода.
Если её не вызвать, человек может вставить <script> в комментарий, и этот код выполнится в браузере каждого посетителя страницы (уязвимость XSS).

![alt text](screenshots/09-react-list.png)

![alt text](screenshots/10-react-edit.png)

![alt text](screenshots/11-react-delete.png)

+----------------------+------------------------------------------+------------------------------------------+
| Пункт                | Vanilla JS                               | React                                    |
+----------------------+------------------------------------------+------------------------------------------+
| Где хранится         | В DOM-элементах (element.value) и        | В хуках useState внутри компонентов      |
| состояние            | глобальных переменных                    | (реактивное хранилище)                   |
+----------------------+------------------------------------------+------------------------------------------+
| Как обновляется      | Вручную: innerHTML = ...map().join('')   | Через setItems(): React сам пересчитывает|
| список               | перезаписывает весь контейнер            | виртуальный DOM и точечно обновляет      |
+----------------------+------------------------------------------+------------------------------------------+
| Как реализовано      | Ручная подмена элементов: createElement, | Условный рендеринг в JSX:                |
| редактирование       | replaceChild, флаги состояния            | {editMode ? <input/> : <p>} + useState   |
+----------------------+------------------------------------------+------------------------------------------+
| Защита от XSS        | Вручную: функция esc() экранирует через  | Автоматически: React экранирует все      |
|                      | textContent перед innerHTML              | значения в {}, кроме dangerouslySet...   |
+----------------------+------------------------------------------+------------------------------------------+

![alt text](screenshots/12-network.png)
4 запросов, comments (Type: fetch) - это основной запрос к API (к данным по комментариями)

![alt text](screenshots/13-source-ssr.png)

![alt text](screenshots/14-source-csr.png)
Сервер отправляет только пустой HTML-шаблон со скриптами, а сам контент загружается браузером через JavaScript (React) уже после открытия страницы, поэтому «View Source» показывает состояние страницы до выполнения скриптов. Большинство протых ботов увидят пустую страницу и проигнорируют сайт.

![alt text](screenshots/15-xss.png)
Vanilla JS:
Надо явно использовать textContent вместо innerHTML или создавать функции экранирования (как esc()), которые превращают < и > в безопасные HTML-сущности перед вставкой в DOM.
React:
Защита автоматическая - React экранирует все значения в JSX-выражениях {variable} по умолчанию, превращая опасные символы в безопасный текст без участия разработчика.
React надёжнее, так как защита встроена по умолчанию и не зависит от внимательности разработчика

+--------------------------------+------------------+------------------+------------------+
|                                |   SSR (PHP)      |   vanilla JS     |   React          |
+--------------------------------+------------------+------------------+------------------+
| Кто рендерит HTML              | Сервер (PHP)     | Браузер (JS)     | Браузер (React)  |
+--------------------------------+------------------+------------------+------------------+
| Формат ответа сервера          | Готовый HTML     | HTML-шаблон +    | HTML-шаблон +    |
|                                |                  | JSON (API)       | JSON (API)       |
+--------------------------------+------------------+------------------+------------------+
| View Source: данные видны?     | Да               | Нет              | Нет              |
+--------------------------------+------------------+------------------+------------------+
| Перезагрузка при отправке      | Да (полная)      | Нет (AJAX)       | Нет              |
|                                |                  |                  | (preventDefault) |
+--------------------------------+------------------+------------------+------------------+
| Защита от XSS                  | Ручная           | Ручная           | Автоматическая   |
|                                | (htmlspecialchars) (textContent,    | (React экранирует|
|                                |                  | esc())           | по умолчанию)    |
+--------------------------------+------------------+------------------+------------------+
| Сложность кода                 | Низкая           | Средняя          | Средняя/Высокая  |
+--------------------------------+------------------+------------------+------------------+
